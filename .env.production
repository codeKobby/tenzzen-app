# This file provides fallback values for the build process
# These values will be overridden by the actual environment variables in Vercel

# Clerk Authentication - Fallback values for build only
# These are placeholder values that will be replaced by actual values in Vercel
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_placeholder_for_build_only
CLERK_SECRET_KEY=sk_placeholder_for_build_only

# Supabase - Fallback values for build only
NEXT_PUBLIC_SUPABASE_URL=https://placeholder-for-build-only.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder_for_build_only
SUPABASE_SERVICE_ROLE_KEY=placeholder_for_build_only

# ADK Service - Fallback values for build only
NEXT_PUBLIC_ADK_SERVICE_URL=http://localhost:8001
NEXT_PUBLIC_ADK_SERVICE_TIMEOUT=300000

# YouTube API - Fallback value for build only
YOUTUBE_API_KEY=placeholder_for_build_only
