export const SIDEBAR_WIDTH = 280

// General transition settings for UI animations
export const TRANSITION_DURATION = 300; // in milliseconds
export const TRANSITION_TIMING = "ease-in-out";

// App-wide feature flags
export const FEATURES = {
  COLLABORATIVE_NOTES: false,
  COURSE_GENERATION: true,
  PROJECT_SUBMISSION: false,
};

// API endpoints
export const API_ENDPOINTS = {
  NOTES: "/api/notes",
  COURSES: "/api/courses",
  YOUTUBE: "/api/youtube",
};

// Default options
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_TAGS_DISPLAY = 10;
