# Codebase Overview

The repository root is `c:/Users/kgeor/OneDrive/Desktop/Code/tenzzen-app`.
Key directories:

- `app/` – Next.js App Router pages and layouts.
- `components/` – Reusable UI components.
- `convex/` – Convex schema, queries, and mutations.
- `actions/` – Server actions for external API calls.
- `lib/ai/` – AI client configuration and prompts.
- `hooks/` – Custom React hooks.
- `docs/` – Project documentation.

This overview can be used by the skill to understand the project structure.
