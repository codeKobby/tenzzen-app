// use youtubei.js to fetch transcription in segments with timestamp and text

// ensure to cater search for both single video and playlist so for playlist, you'd have to get individual video ids

// check if transcript already exists in db,

// if not, fetch

// and save to db(caching)

// return transcript